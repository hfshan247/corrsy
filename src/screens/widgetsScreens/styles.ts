import {StyleSheet} from 'react-native';
import {GlobalStyles} from '../../utils/globalStyles';
export const styles = StyleSheet.create({
  splashMain: {
    display: 'flex',
    width: '100%',
    height: '100%',
  },
});
export const commonStyles = {
  splashMainView: [GlobalStyles.rowCenter],
};
